services:
  - type: web
    name: antigravity-backend
    runtime: node
    plan: free
    repo: https://github.com/ezequielmanchula/catalogo-libreria
    branch: main
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
